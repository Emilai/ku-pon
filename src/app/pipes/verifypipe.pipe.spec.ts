import { VerifypipePipe } from './verifypipe.pipe';

describe('VerifypipePipe', () => {
  it('create an instance', () => {
    const pipe = new VerifypipePipe();
    expect(pipe).toBeTruthy();
  });
});
